import { ClientOptions } from './client/createClient';
import { GraphqlOperation } from './client/generateGraphqlOperation';
export interface Fetcher {
    (gql: GraphqlOperation): Promise<any>;
}
export declare type BatchOptions = {
    batchInterval?: number;
    maxBatchSize?: number;
};
export declare const createFetcher: ({ url, headers, fetcher, batch, ...rest }: ClientOptions) => Fetcher;
//# sourceMappingURL=fetcher.d.ts.map