import { AuthenticatedClient, AuthenticatedClientOptions } from '../libs/AuthenticatedClient';
type ProjectsClientOptions = AuthenticatedClientOptions & {
    graphqlServiceApiUrl: string;
};
export declare class ProjectsClient extends AuthenticatedClient {
    private client?;
    private graphqlServiceApiUrl;
    private getClient;
    constructor(options: ProjectsClientOptions);
    create: ({ name }: {
        name: string;
    }) => Promise<Pick<{
        id: string;
        name: string;
        __typename: "Project";
    }, "id" | "name">>;
    get: ({ id }: {
        id: string;
    }) => Promise<Pick<{
        id: string;
        name: string;
        __typename: "Project";
    }, "id" | "name">>;
    list: () => Promise<Pick<{
        id: string;
        name: string;
        __typename: "Project";
    }, "id" | "name">[]>;
}
export {};
//# sourceMappingURL=projects.d.ts.map